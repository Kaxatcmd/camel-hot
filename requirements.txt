# DJ Harmonic Analyzer - Python Dependencies
# Install with: pip install -r requirements.txt

# Core Dependencies (Required)
librosa>=0.10.0    # Audio analysis library for detecting keys and BPM
PyQt5>=5.15.0      # Professional GUI framework for the application
matplotlib>=3.5.0  # For generating the Camelot wheel visualization

# Note: librosa automatically installs its dependencies:
# - numpy
# - scipy
# - scikit-learn
# - soundfile
# - audioread
# And other supporting libraries

# Audio format support — ships its own static ffmpeg binary (MP3, AAC, etc.)
imageio-ffmpeg>=0.5.0

# Audio playback (required for player bar and Showoff! transition engine)
python-vlc>=3.0.0    # VLC-based audio player for compatibility tab

