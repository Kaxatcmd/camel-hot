# DJ Harmonic Analyzer - Python Dependencies
# Install with: pip install -r requirements.txt

# Core audio analysis
librosa>=0.10.0        # Key and BPM detection (installs numpy, scipy, scikit-learn, etc.)
audioread>=3.0.0       # Multi-backend audio loader used by librosa
soundfile>=0.12.0      # WAV/FLAC I/O; bundles libsndfile (includes DLL on Windows)

# CRITICAL: static ffmpeg binary — required for MP3/AAC/OGG decoding.
# Without this, librosa cannot analyze MP3 files on any platform.
imageio-ffmpeg>=0.5.0

# GUI
PyQt5>=5.15.0          # Application UI framework
matplotlib>=3.5.0      # Camelot wheel visualization

# Audio playback (optional — app degrades gracefully if VLC is not installed)
python-vlc>=3.0.0      # VLC wrapper; requires VLC to be installed system-wide

